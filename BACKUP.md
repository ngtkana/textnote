# バックアップ・エクスポート/インポートガイド

TextNoteのデータは**IndexedDB（ブラウザ内のローカルストレージ）**に保存されています。  
定期的にバックアップを取ることで、データの損失を防ぐことができます。

---

## 📤 エクスポート（バックアップ作成）

### 手順

1. アプリ右上の「**エクスポート**」ボタン（⤓）をクリック
2. `textnote-backup-XXXXXXXXX.json` というファイルがダウンロードされます
3. このJSONファイルを安全な場所に保存してください

### エクスポートされるデータ

```json
[
  {
    "id": "1732892345678-abc123",
    "title": "買い物リスト",
    "order": 0,
    "content": "牛乳\nパン\n卵",
    "createdAt": 1732892345678,
    "updatedAt": 1732892456789
  },
  {
    "id": "1732892367890-def456",
    "title": "やることリスト",
    "order": 1,
    "content": "掃除\n洗濯",
    "createdAt": 1732892367890,
    "updatedAt": 1732892478901
  }
]
```

**含まれるもの**:
- すべてのファイル（TextFile）
- タイトル、本文、作成日時、更新日時
- ファイルの並び順

**含まれないもの**:
- アプリの設定（将来実装予定）
- ブラウザのキャッシュ

---

## 📥 インポート（バックアップ復元）

### 手順

1. アプリ右上の「**インポート**」ボタン（⤴）をクリック
2. ファイル選択ダイアログが開きます
3. 以前エクスポートした `.json` ファイルを選択
4. 確認ダイアログが表示されます：
   ```
   X件のファイルをインポートします。
   既存のデータは削除されます。よろしいですか？
   ```
5. 「OK」をクリックするとインポートが実行されます

### ⚠️ 重要な注意点

**既存データは完全に削除されます**

インポートすると、現在のIndexedDB内のデータは**すべて削除**され、インポートしたデータに置き換わります。

**対策**:
- インポート前に現在のデータをエクスポート
- 誤って実行した場合は、すぐにブラウザの「戻る」は効かないため、直前のバックアップをインポートし直す

---

## 🔄 バックアップのベストプラクティス

### 1. 定期的なバックアップ

**推奨頻度**:
- **毎日使う場合**: 週1回
- **重要なデータがある場合**: 使用後毎回
- **大量のテキストを入力した後**: すぐに

### 2. バックアップファイルの保存場所

**推奨**:
- クラウドストレージ（Google Drive、Dropbox、iCloud Driveなど）
- 複数の場所に保存（冗長化）

**非推奨**:
- ダウンロードフォルダのみ（誤って削除しやすい）
- ブラウザのキャッシュ（ブラウザ削除時に消える）

### 3. バックアップファイル名の管理

エクスポートされるファイル名:
```
textnote-backup-1732892345678.json
                └─ Unix timestamp（ミリ秒）
```

**推奨**: 日付が分かりやすいようにリネーム
```
textnote-backup-2026-05-29.json
textnote-backup-2026-06-05.json
```

---

## 🛡️ データ復旧シナリオ

### シナリオ1: ブラウザのキャッシュを誤って削除

**対処法**:
1. 最新のバックアップファイルを探す
2. 「インポート」ボタンからファイルを選択
3. データが復元されます

### シナリオ2: 別のデバイスでデータを使いたい

**対処法**:
1. 元のデバイスで「エクスポート」
2. バックアップファイルをクラウドストレージ経由で転送
3. 新しいデバイスで「インポート」

### シナリオ3: 誤ってファイルを削除

**対処法**:
- **バックアップがある場合**: インポートで復元
- **バックアップがない場合**: 復元不可能 ❌

→ **定期バックアップが重要**

---

## 📊 エクスポート/インポートの仕組み

### エクスポート処理

```typescript
// 1. IndexedDBから全データを取得
const files = await storage.getAllFiles();

// 2. JSON形式に変換（整形済み）
const jsonString = JSON.stringify(files, null, 2);

// 3. Blobオブジェクトを作成
const blob = new Blob([jsonString], { type: 'application/json' });

// 4. ダウンロードリンクを生成してクリック
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = `textnote-backup-${Date.now()}.json`;
a.click();
```

### インポート処理

```typescript
// 1. ファイルを読み込み
const text = await file.text();

// 2. JSON解析
const data = JSON.parse(text);

// 3. データ検証
if (!Array.isArray(data)) {
  throw new Error('無効なデータ形式');
}

// 4. IndexedDBをクリア
await storage.clear();

// 5. データを1件ずつ保存
for (const file of data) {
  await storage.createFile(file);
}
```

---

## 🔧 高度な使い方

### 1. JSONファイルの手動編集

エクスポートしたJSONファイルは**テキストエディタで編集可能**です。

```json
[
  {
    "id": "1732892345678-abc123",
    "title": "買い物リスト",  ← タイトル変更可能
    "content": "牛乳\nパン",  ← 本文変更可能
    "order": 0,              ← 順序変更可能
    "createdAt": 1732892345678,
    "updatedAt": 1732892456789
  }
]
```

**注意**:
- `id` フィールドは変更しないでください（重複すると上書きされます）
- JSON形式を壊さないように注意
- タイムスタンプはUnix時間（ミリ秒）

### 2. 複数のバックアップをマージ

2つのバックアップファイルを統合する場合:

```json
// backup1.json
[
  { "id": "1", "title": "リスト1", ... }
]

// backup2.json
[
  { "id": "2", "title": "リスト2", ... }
]

// merged.json（手動で統合）
[
  { "id": "1", "title": "リスト1", ... },
  { "id": "2", "title": "リスト2", ... }
]
```

`merged.json` をインポートすれば、両方のデータが復元されます。

### 3. 特定のファイルのみ復元

全体のバックアップから特定のファイルのみ復元:

```json
// 元のbackup.json
[
  { "id": "1", "title": "削除したくない", ... },
  { "id": "2", "title": "復元したい", ... },
  { "id": "3", "title": "削除したくない", ... }
]

// 編集後（id=2のみ残す）
[
  { "id": "2", "title": "復元したい", ... }
]
```

このファイルをインポート → 他のデータは消えて、id=2のみが残ります。

**⚠️ 注意**: 既存データは消えるので、先にエクスポートしてから作業してください。

---

## 🚀 将来の機能（予定）

### 自動バックアップ

- ローカルストレージに毎日自動保存
- 設定で有効/無効を切り替え可能
- 最大5世代まで保持

### クラウド同期（Phase 3）

- Google認証でFirestoreと同期
- リアルタイムでマルチデバイス対応
- ローカルバックアップとクラウドバックアップの選択可能

### Markdown形式エクスポート

```markdown
# 買い物リスト

牛乳
パン
卵

---

# やることリスト

掃除
洗濯
```

各ファイルを見出し付きのMarkdownファイルとしてエクスポート。

---

## ❓ よくある質問

### Q: エクスポートしたファイルは他の人と共有できる？

A: できますが、**あなたのテキストデータがすべて含まれています**。  
プライベートな内容が含まれていないか確認してから共有してください。

### Q: 古いバージョンのバックアップは使える？

A: 基本的には互換性がありますが、将来的にデータ構造が変わる可能性があります。  
その場合はマイグレーション処理が自動で実行されます。

### Q: インポートに失敗した場合は？

A: 以下を確認してください:
- JSONファイルが壊れていないか
- 配列形式になっているか（`[...]`）
- 各ファイルに必須フィールド（`id`, `title`, `content`, `order`, `createdAt`, `updatedAt`）があるか

エラーメッセージがブラウザのコンソール（F12 → Console）に表示されます。

---

**最終更新**: 2026年5月29日
